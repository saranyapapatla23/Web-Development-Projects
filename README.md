# Project 1 — Text Analyzer

A beginner-friendly JavaScript project that allows users to enter text and perform different text manipulation and analysis operations.

## 🎥 Project Demo

https://github.com/user-attachments/assets/63369bce-106f-4f37-b9d9-5333028ad614

## ✨ Features

* Count the total number of characters
* Count characters excluding spaces
* Convert text to uppercase
* Convert text to lowercase
* Extract a portion of text using `slice()`
* Replace text using `replace()`
* Replace all occurrences using `replaceAll()`
* Find the first character
* Find the last character
* Reverse the entered text
* Check whether text contains a specific word
* Check whether text starts with specific text
* Check whether text ends with specific text
* Clear the input and result

## 🛠️ Technologies Used

* HTML
* CSS
* JavaScript

## 📚 JavaScript Concepts Practiced

* DOM Manipulation
* `getElementById()`
* `addEventListener()`
* Reading input using `.value`
* Updating content using `.textContent`
* String properties and methods
* `.length`
* `.toUpperCase()`
* `.toLowerCase()`
* `.slice()`
* `.replace()`
* `.replaceAll()`
* `.includes()`
* `.startsWith()`
* `.endsWith()`
* `.split()`
* `.reverse()`
* `.join()`
* `prompt()`
* `Number()`
* Conditional statements
* Functions

## 🎯 Purpose
This project was built as part of my JavaScript learning journey to strengthen my understanding of strings, DOM manipulation, events, user input, and basic JavaScript logic.

# Project 2 — BMI Calculator

A beginner-friendly BMI Calculator built using HTML, CSS, and JavaScript. The application takes the user's height and weight, calculates their BMI using a JavaScript function, and displays a general BMI status using conditional statements.

## 📸 Project Demo

https://github.com/user-attachments/assets/35aa7b74-8590-4148-8fc1-baa72796f777

## ✨ Features

* Enter height in centimeters
* Enter weight in kilograms
* Calculate BMI
* Display BMI value up to 2 decimal places
* Display general BMI status
* Reset the input and results
* Press Enter after entering height to move to the weight field
* Press Enter after entering weight to calculate BMI

## 🛠️ Technologies Used

* HTML
* CSS
* JavaScript

## 📚 JavaScript Concepts Practiced

* Functions
* Function parameters
* Return values
* Calling functions
* DOM manipulation
* `getElementById()`
* `addEventListener()`
* `.value`
* `.textContent`
* `Number()`
* `toFixed()`
* `if / else if / else`
* Comparison operators
* Keyboard events
* `event.key`
* `.focus()`

## 🧮 BMI Formula

```text
BMI = weight / (height in meters)²
```

The application converts the entered height from centimeters to meters before calculating BMI.

## 🎯 Purpose

This project was built as part of my JavaScript learning journey to practice functions, return values, conditional statements, DOM manipulation, and user input handling.

# 🎓Project 3 —  Student Grading System

A simple and interactive **Student Grading System** built using HTML, CSS, and JavaScript.

This project takes the maximum marks, student's obtained marks, and class average as input and calculates the student's percentage, grade, and performance compared with the class average.

## 🎥 Demo

https://github.com/user-attachments/assets/c17f4726-ca48-47d6-8d92-62db0200e6d7

## 🚀 Features

* Calculate student's percentage
* Generate grades from **A+ to F**
* Compare student's performance with the class average
* Dynamic progress bar based on percentage
* Progress bar changes color according to performance
* Input validation
* Reset functionality
* Clean and responsive user interface

## 🛠️ Technologies Used

* **HTML5**
* **CSS3**
* **JavaScript**

## 📚 JavaScript Concepts Practiced

* Variables using `const`
* Number conversion using `Number()`
* Arithmetic operators
* Conditional statements
* Functions and return values
* Function parameters and arguments
* DOM selection using `getElementById()`
* Reading input using `.value`
* Updating content using `.textContent`
* Event handling using `addEventListener()`
* Dynamic CSS using `.style`
* Dynamic classes using `.classList`
* Input validation
* Reset functionality
* `toFixed()` for formatting percentages

## 🧮 Grading Criteria

| Percentage | Grade |
| ---------- | ----- |
| 90% - 100% | A+    |
| 80% - 89%  | A     |
| 70% - 79%  | B     |
| 60% - 69%  | C     |
| 50% - 59%  | D     |
| Below 50%  | F     |

## 📊 How It Works

1. Enter the maximum marks.
2. Enter the student's obtained marks.
3. Enter the class average.
4. Click **Calculate Result**.
5. The percentage and grade are calculated.
6. The progress bar represents the student's performance.
7. The result is compared with the class average.
8. Click **Reset** to clear the inputs and results.

## 💡 What I Learned

This project helped me understand how **JavaScript interacts with HTML through the DOM**.
I practiced selecting elements, reading user input, performing calculations, validating data, updating the webpage dynamically, handling events, and changing CSS using JavaScript.


